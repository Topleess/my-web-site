# 🐳 Запуск проекта через Docker (в одну команду!)

## 🎯 Преимущества Docker

✅ Всё работает из коробки  
✅ Не нужно устанавливать Python, Node.js, PostgreSQL  
✅ Одна команда для запуска  
✅ Одна команда для обновления  
✅ Работает одинаково на любом сервере  

---

## 📋 Что вам нужно

**Только Docker!**

### На сервере (Linux):
```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Проверка
docker --version
docker compose version
```

### На Windows (для локальной разработки):
Скачайте и установите [Docker Desktop](https://www.docker.com/products/docker-desktop/)

---

## 🚀 Быстрый старт (3 шага!)

### Шаг 1: Загрузите проект на сервер

**Вариант A: Через Git**
```bash
cd ~
git clone https://github.com/ваш-username/ваш-репозиторий.git portfolio
cd portfolio
```

**Вариант B: Через SCP (с вашего компьютера)**
```powershell
# На Windows PowerShell
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site"
scp -r . username@ваш-сервер.com:~/portfolio
```

**Вариант C: Через WinSCP/FileZilla**
Просто скопируйте всю папку проекта на сервер.

---

### Шаг 2: Создайте .env файл (опционально)

```bash
cd ~/portfolio/backend
nano .env
```

Вставьте (или оставьте пустым, используются дефолтные настройки из docker-compose.yml):
```env
DATABASE_URL=postgres://portfolio_user:secret123@db:5432/portfolio_prod
DB_HOST=db
DB_PORT=5432
DB_NAME=portfolio_prod
DB_USER=portfolio_user
DB_PASSWORD=secret123
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=*
ENV=production
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

**⚠️ ВАЖНО:** Если вы не создадите .env, используются настройки из docker-compose.yml (они уже правильные).

---

### Шаг 3: Запустите всё одной командой! 🎉

```bash
cd ~/portfolio
docker compose up -d --build
```

**Что происходит:**
1. 🐘 Docker скачивает и запускает PostgreSQL
2. 🐍 Docker собирает и запускает Python бэкенд
3. ⚛️ Docker собирает React фронтенд (npm install + npm run build)
4. 🌐 Docker запускает Nginx для раздачи сайта
5. 🛠️ Docker запускает pgAdmin (админка базы)

**Ждите 2-5 минут** (первый запуск дольше, потому что Docker всё скачивает и собирает).

---

### Шаг 4: Заполните базу тестовыми данными (один раз)

```bash
docker compose exec backend python -m app.seed_data
```

Вы увидите:
```
✅ Создан проект: Корпоративный сайт (ID: 1)
✅ Создан проект: Мобильное приложение (ID: 2)
...
```

---

## ✅ Готово! Проверка

Откройте в браузере:

| Что | Адрес | Описание |
|-----|-------|----------|
| **Ваш сайт** | `http://ваш-ip` или `http://ваш-домен.com` | Главная страница портфолио |
| **API Документация** | `http://ваш-ip/docs` | Swagger UI для управления проектами |
| **Админка БД** | `http://ваш-ip:5050` | pgAdmin (admin@admin.com / admin) |
| **Проверка API** | `http://ваш-ip/api/health` | Должно вернуть `{"message":"OK"}` |

---

## 🛠️ Полезные команды

### Посмотреть статус всех контейнеров:
```bash
docker compose ps
```

Должно быть:
```
NAME                   STATUS
portfolio_backend      Up (healthy)
portfolio_db           Up (healthy)
portfolio_frontend     Up
portfolio_pgadmin      Up
```

---

### Посмотреть логи:

**Все логи сразу:**
```bash
docker compose logs -f
```

**Только бэкенд:**
```bash
docker compose logs -f backend
```

**Только база данных:**
```bash
docker compose logs -f db
```

---

### Перезапустить всё:
```bash
docker compose restart
```

---

### Остановить всё:
```bash
docker compose down
```

---

### Остановить и удалить всё (включая базу данных!):
```bash
docker compose down -v
```

---

## 🔄 Как обновить сайт после изменений

Вы что-то поменяли в коде? Просто:

```bash
cd ~/portfolio
git pull  # если используете Git
docker compose up -d --build
```

Docker автоматически:
- ✅ Пересоберёт фронтенд (если изменился)
- ✅ Пересоберёт бэкенд (если изменился)
- ✅ Перезапустит контейнеры
- ✅ База данных останется нетронутой!

---

## 📝 Как редактировать проекты

### Способ 1: Через Swagger UI (САМЫЙ ПРОСТОЙ)

Откройте: `http://ваш-ip/docs`

**Добавить проект:**
1. Найдите `POST /api/projects`
2. Нажмите **Try it out**
3. Вставьте JSON:
```json
{
  "title": "Новый проект",
  "category": "Веб-разработка",
  "status": "Завершен",
  "year": "2024",
  "image": "https://images.unsplash.com/photo-1...",
  "description": "Описание проекта",
  "client": "Название клиента",
  "role": "Моя роль",
  "images": []
}
```
4. Нажмите **Execute**

**Изменить проект:**
- Используйте `PUT /api/projects/{id}`

**Удалить проект:**
- Используйте `DELETE /api/projects/{id}`

---

### Способ 2: Через pgAdmin

Откройте: `http://ваш-ip:5050`

**Первый вход:**
1. Email: `admin@admin.com`
2. Password: `admin`

**Подключение к базе:**
1. Правый клик → **Register** → **Server**
2. General:
   - Name: `Portfolio DB`
3. Connection:
   - Host: `db` (именно так, не localhost!)
   - Port: `5432`
   - Database: `portfolio_prod`
   - Username: `portfolio_user`
   - Password: `secret123`
4. **Save**

Теперь можете редактировать таблицу `projects` как Excel!

---

### Способ 3: Через командную строку (для продвинутых)

```bash
# Зайти в контейнер бэкенда
docker compose exec backend bash

# Открыть Python консоль
python -m app.seed_data
```

---

## 🐛 Решение проблем

### Проблема: Порт 80 занят

**Причина:** На сервере уже работает Nginx или Apache

**Решение:**
```bash
# Остановите старый Nginx
sudo systemctl stop nginx
sudo systemctl disable nginx

# Или измените порт в docker-compose.yml
# Строка:  - "80:80"
# Замените на:  - "8080:80"
# Тогда сайт будет на http://ваш-ip:8080
```

---

### Проблема: Контейнеры не запускаются

**Посмотрите логи:**
```bash
docker compose logs
```

**Проверьте, что Docker работает:**
```bash
docker ps
```

**Пересоздайте всё заново:**
```bash
docker compose down -v
docker compose up -d --build
```

---

### Проблема: База данных пустая

**Запустите заполнение данными:**
```bash
docker compose exec backend python -m app.seed_data
```

---

### Проблема: Не могу зайти в pgAdmin

**Проверьте, что контейнер работает:**
```bash
docker compose ps pgadmin
```

**Проверьте логи:**
```bash
docker compose logs pgadmin
```

**Откройте порт в firewall (если нужно):**
```bash
sudo ufw allow 5050/tcp
```

---

## 🔒 Настройка HTTPS (SSL)

### С доменом:

```bash
# Установите Certbot
sudo apt install certbot -y

# Остановите Docker на время получения сертификата
docker compose down

# Получите сертификат
sudo certbot certonly --standalone -d ваш-домен.com

# Измените docker-compose.yml (добавьте в секцию frontend):
# volumes:
#   - /etc/letsencrypt:/etc/letsencrypt:ro
# ports:
#   - "443:443"

# И измените nginx.conf (добавьте секцию для HTTPS)

# Запустите обратно
docker compose up -d
```

---

## 📊 Мониторинг

### Посмотреть использование ресурсов:
```bash
docker stats
```

### Посмотреть все контейнеры:
```bash
docker compose ps -a
```

### Освободить место (удалить неиспользуемые образы):
```bash
docker system prune -a
```

---

## 📦 Бэкап базы данных

### Создать бэкап:
```bash
docker compose exec db pg_dump -U portfolio_user portfolio_prod > backup_$(date +%Y%m%d).sql
```

### Восстановить из бэкапа:
```bash
cat backup_20240115.sql | docker compose exec -T db psql -U portfolio_user -d portfolio_prod
```

---

## 🎯 Чек-лист запуска

- [ ] Docker установлен на сервере
- [ ] Код скопирован на сервер
- [ ] Выполнена команда `docker compose up -d --build`
- [ ] База заполнена данными (`seed_data`)
- [ ] Сайт открывается в браузере
- [ ] API работает (проверка `/api/health`)
- [ ] Swagger UI доступен (`/docs`)
- [ ] pgAdmin доступен (`:5050`)

---

## 🎉 Готово!

Теперь ваш сайт работает 24/7 и обновляется одной командой:

```bash
docker compose up -d --build
```

**Никаких systemd, прав доступа, конфликтов версий!** 🚀

---

## 📞 Нужна помощь?

Смотрите логи:
```bash
docker compose logs -f
```

Перезапустите:
```bash
docker compose restart
```

Пересоздайте всё:
```bash
docker compose down
docker compose up -d --build
```
