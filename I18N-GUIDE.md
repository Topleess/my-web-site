# 🌍 Руководство по интернационализации (i18n)

## 📋 Обзор

Ваш проект теперь полностью поддерживает интернационализацию (i18n) на русском (RU) и английском (EN) языках.

## ✅ Что было реализовано

### ЧАСТЬ 1: Фронтенд (Настройка i18n)

#### Установленные библиотеки:
- `i18next` - основная библиотека для интернационализации
- `react-i18next` - интеграция с React
- `i18next-http-backend` - загрузка переводов из файлов
- `i18next-browser-languagedetector` - автоматическое определение языка браузера

#### Созданные файлы:

1. **`frontend/src/i18n.ts`** - конфигурация i18n
   - Автоматическое определение языка по настройкам браузера
   - Fallback на русский язык ('ru')
   - Поддержка кэширования выбранного языка в localStorage

2. **Файлы переводов:**
   - `frontend/public/locales/ru/translation.json` - русские переводы
   - `frontend/public/locales/en/translation.json` - английские переводы

#### Обновленные компоненты:
- ✅ `Header.tsx` - добавлен переключатель языков (RU/EN) с иконкой глобуса
- ✅ `Projects.tsx` - мультиязычное отображение проектов
- ✅ `ProjectPage.tsx` - мультиязычная страница проекта
- ✅ `Hero.tsx` - локализованный главный баннер
- ✅ `Contact.tsx` - локализованная секция контактов
- ✅ `Footer.tsx` - локализованный футер

### ЧАСТЬ 2: Бэкенд и База Данных

#### Обновленные файлы:

1. **`backend/app/models.py`**
   - Добавлены поля: `title_en` и `description_en`

2. **`backend/app/schemas.py`**
   - Обновлены схемы `ProjectBase`, `ProjectCreate`, `ProjectUpdate`, `ProjectResponse`
   - Добавлены поля для английских переводов

3. **SQL-миграция**
   - Создан файл `backend/migrations/add_i18n_fields.sql`

### ЧАСТЬ 3: Интеграция на Фронтенде

- Компоненты теперь автоматически отображают контент на выбранном языке
- Функция `getLocalizedField()` выбирает правильное поле в зависимости от языка
- Переключатель языков в шапке сайта (десктоп и мобильная версия)

---

## 🚀 Использование

### Переключение языка

Пользователи могут переключать язык:
1. **Нажав на кнопку с глобусом** в шапке сайта (показывает текущий язык: RU/EN)
2. Язык автоматически сохраняется в localStorage

### Определение языка

Система автоматически определяет язык в следующем порядке:
1. Настройки браузера (navigator.language)
2. Сохраненный язык в localStorage
3. Fallback на русский язык

---

## 📝 Выполнение SQL-миграции

Чтобы добавить поля для английских переводов в базу данных, выполните:

### Вариант 1: Напрямую в PostgreSQL

```sql
ALTER TABLE projects 
ADD COLUMN title_en VARCHAR(255) NULL;

ALTER TABLE projects 
ADD COLUMN description_en TEXT NULL;
```

### Вариант 2: Через файл миграции

```bash
# В PowerShell
cd backend
psql -U your_username -d your_database -f migrations/add_i18n_fields.sql
```

---

## 🛠️ Добавление новых переводов

### 1. Добавление ключа перевода

Откройте файлы переводов и добавьте новый ключ:

**`frontend/public/locales/ru/translation.json`**
```json
{
  "mySection": {
    "newKey": "Новый текст"
  }
}
```

**`frontend/public/locales/en/translation.json`**
```json
{
  "mySection": {
    "newKey": "New text"
  }
}
```

### 2. Использование в компонентах

```tsx
import { useTranslation } from 'react-i18next';

const MyComponent: React.FC = () => {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('mySection.newKey')}</h1>
    </div>
  );
};
```

### 3. Получение текущего языка

```tsx
import { useTranslation } from 'react-i18next';

const MyComponent: React.FC = () => {
  const { i18n } = useTranslation();
  
  return (
    <div>
      Current language: {i18n.language}
    </div>
  );
};
```

---

## 📊 Структура переводов

```
frontend/
├── public/
│   └── locales/
│       ├── ru/
│       │   └── translation.json
│       └── en/
│           └── translation.json
└── src/
    └── i18n.ts
```

---

## 🎨 Мультиязычные проекты

### Добавление английских переводов к проектам

При создании или обновлении проекта через API, добавьте поля:

```json
{
  "title": "Название проекта",
  "description": "Описание проекта",
  "title_en": "Project Title",
  "description_en": "Project Description"
}
```

### Отображение на фронтенде

Компонент автоматически выберет правильное поле:
- Если язык 'en' и `title_en` существует → показывает `title_en`
- Иначе → показывает `title`

---

## 🧪 Тестирование

1. Запустите проект:
```bash
cd frontend
npm run dev
```

2. Откройте браузер и проверьте:
   - Переключатель языков в шапке работает
   - Контент меняется при переключении
   - Язык сохраняется после перезагрузки страницы

---

## 📚 Доступные ключи переводов

### Header
- `header.title` - Заголовок сайта
- `header.projects` - Проекты
- `header.about` - Обо мне
- `header.contact` - Связаться

### Projects
- `projects.title` - Мои проекты
- `projects.viewProject` - Посмотреть проект
- `projects.category` - Категория
- `projects.status` - Статус
- `projects.year` - Год
- `projects.client` - Клиент
- `projects.role` - Роль

### Contact
- `contact.title` - Контакты
- `contact.description` - Описание секции

### Common
- `common.switchLanguage` - Переключить язык
- `common.loading` - Загрузка...
- `common.error` - Произошла ошибка

---

## 🐛 Устранение неполадок

### Переводы не загружаются

1. Убедитесь, что файлы переводов находятся в `public/locales/`
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что `i18n.ts` импортирован в `index.tsx`

### Язык не сохраняется

Проверьте, что в `i18n.ts` настроен `localStorage`:
```typescript
detection: {
  caches: ['localStorage'],
  lookupLocalStorage: 'i18nextLng',
}
```

### Компонент не обновляется при смене языка

Убедитесь, что компонент использует хук `useTranslation()`:
```typescript
const { t, i18n } = useTranslation();
```

---

## 🎯 Рекомендации

1. **Всегда используйте ключи переводов** вместо хардкода текста
2. **Группируйте переводы** по секциям (header, footer, contact и т.д.)
3. **Добавляйте английские переводы** для всех проектов в базе данных
4. **Тестируйте оба языка** перед деплоем

---

## 📞 Дополнительная информация

- [Документация i18next](https://www.i18next.com/)
- [Документация react-i18next](https://react.i18next.com/)

---

**Создано:** 2026-01-10  
**Статус:** ✅ Полностью реализовано
