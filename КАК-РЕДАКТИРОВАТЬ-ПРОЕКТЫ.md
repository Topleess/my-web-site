# 📝 Как редактировать проекты на сайте

Это подробное руководство о том, как добавлять, редактировать и удалять проекты в вашем портфолио.

## 📚 Содержание

1. [Способы редактирования](#способы-редактирования)
2. [Способ 1: Через API (Swagger UI)](#способ-1-через-api-swagger-ui)
3. [Способ 2: Прямое редактирование базы данных](#способ-2-прямое-редактирование-базы-данных)
4. [Способ 3: Через Python скрипт](#способ-3-через-python-скрипт)
5. [Структура проекта](#структура-проекта)

---

## Способы редактирования

У вас есть **3 способа** редактировать проекты:

### ✅ Способ 1: Через API (Swagger UI) - **РЕКОМЕНДУЕТСЯ**
- ➕ Самый простой и наглядный
- ➕ Не требует знаний программирования
- ➕ Графический интерфейс
- ➖ Требует запущенный бэкенд

### ✅ Способ 2: Прямое редактирование БД
- ➕ Быстро для массовых изменений
- ➖ Требует установку DB Browser или знание SQL
- ⚠️ Риск ошибок при ручном редактировании

### ✅ Способ 3: Python скрипт
- ➕ Полный контроль
- ➕ Можно автоматизировать
- ➖ Требует знаний Python

---

## Способ 1: Через API (Swagger UI)

### 🚀 Шаг 1: Запустите бэкенд

```powershell
cd backend
uv run python -m app.main
```

Бэкенд запустится на `http://localhost:8000`

### 📖 Шаг 2: Откройте Swagger UI

Откройте в браузере: **http://localhost:8000/docs**

Вы увидите интерактивную документацию API со всеми доступными операциями.

### ➕ Добавить новый проект

1. Найдите эндпоинт **`POST /api/projects`**
2. Нажмите **"Try it out"**
3. Вставьте JSON с данными проекта:

```json
{
  "title": "Мой новый проект",
  "category": "Разработка",
  "status": "Завершен",
  "year": "2024",
  "image": "https://images.unsplash.com/photo-1498050108023-c5249f4df085",
  "description": "Полное описание проекта. Что было сделано, какие технологии использовались, какие задачи решались.",
  "client": "Название компании-клиента",
  "role": "Моя роль в проекте",
  "images": [
    "https://images.unsplash.com/photo-1461749280684-dccba630e2f6",
    "https://images.unsplash.com/photo-1498050108023-c5249f4df085"
  ]
}
```

4. Нажмите **"Execute"**
5. Проверьте ответ — вы увидите созданный проект с присвоенным `id`

### ✏️ Редактировать существующий проект

1. Найдите эндпоинт **`PUT /api/projects/{project_id}`** или **`PATCH /api/projects/{project_id}`**
2. Нажмите **"Try it out"**
3. Введите **ID проекта** (например, `1`)
4. Вставьте JSON с полями, которые хотите изменить:

```json
{
  "title": "Обновленное название",
  "description": "Новое описание проекта"
}
```

> **Примечание**: При использовании `PATCH` можно указать только те поля, которые нужно изменить. При `PUT` лучше указать все поля.

5. Нажмите **"Execute"**

### 🗑️ Удалить проект

1. Найдите эндпоинт **`DELETE /api/projects/{project_id}`**
2. Нажмите **"Try it out"**
3. Введите **ID проекта**, который хотите удалить
4. Нажмите **"Execute"**
5. Проект будет удален безвозвратно

### 📋 Посмотреть все проекты

1. Найдите эндпоинт **`GET /api/projects`**
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**
4. Вы увидите список всех проектов с их ID

---

## Способ 2: Прямое редактирование базы данных

### Вариант A: Через DB Browser for SQLite (графический интерфейс)

#### Шаг 1: Установите DB Browser

Скачайте и установите: https://sqlitebrowser.org/dl/

#### Шаг 2: Откройте базу данных

1. Запустите **DB Browser for SQLite**
2. **File → Open Database**
3. Выберите файл: `backend/portfolio.db`

#### Шаг 3: Редактируйте данные

1. Перейдите на вкладку **"Browse Data"**
2. Выберите таблицу **`projects`**
3. Дважды кликните на ячейку, чтобы редактировать
4. После изменений нажмите **"Write Changes"**

#### Важные поля:

| Поле | Описание | Пример |
|------|----------|--------|
| `id` | Уникальный ID (не редактируйте!) | `1` |
| `title` | Название проекта | `"FinTech App"` |
| `category` | Категория (только: Дизайн, Разработка, Стартапы, Другое) | `"Разработка"` |
| `status` | Статус (только: В работе, Завершен) | `"Завершен"` |
| `year` | Год завершения | `"2024"` |
| `image` | URL главного изображения | `"https://..."` |
| `description` | Описание проекта | `"Текст..."` |
| `client` | Клиент (необязательно) | `"NeoBank"` |
| `role` | Ваша роль (необязательно) | `"Frontend Lead"` |
| `images` | Дополнительные изображения (JSON массив) | `["url1", "url2"]` |

### Вариант B: Через командную строку (SQL)

```powershell
# Войдите в SQLite
cd backend
sqlite3 portfolio.db

# Посмотреть все проекты
SELECT id, title, category FROM projects;

# Обновить проект
UPDATE projects SET title = 'Новое название', description = 'Новое описание' WHERE id = 1;

# Удалить проект
DELETE FROM projects WHERE id = 5;

# Выйти
.quit
```

---

## Способ 3: Через Python скрипт

Создайте файл `backend/edit_projects.py`:

```python
"""
Скрипт для редактирования проектов
Использование: uv run python edit_projects.py
"""
import asyncio
from tortoise import Tortoise
from app.config import settings
from app.models import Project

async def main():
    # Подключаемся к БД
    await Tortoise.init(
        db_url=settings.DATABASE_URL,
        modules={"models": ["app.models"]}
    )
    
    # ===== ДОБАВИТЬ НОВЫЙ ПРОЕКТ =====
    new_project = await Project.create(
        title="Новый проект",
        category="Разработка",
        status="Завершен",
        year="2024",
        image="https://images.unsplash.com/photo-...",
        description="Описание проекта",
        client="Клиент",
        role="Моя роль",
        images=["https://image1.jpg", "https://image2.jpg"]
    )
    print(f"Создан проект: {new_project.title} (ID: {new_project.id})")
    
    # ===== РЕДАКТИРОВАТЬ СУЩЕСТВУЮЩИЙ ПРОЕКТ =====
    project = await Project.get(id=1)  # Получаем проект с ID=1
    project.title = "Обновленное название"
    project.description = "Новое описание"
    await project.save()
    print(f"Обновлен проект: {project.title}")
    
    # ===== УДАЛИТЬ ПРОЕКТ =====
    project_to_delete = await Project.get(id=5)
    await project_to_delete.delete()
    print(f"Удален проект с ID: 5")
    
    # ===== ПОСМОТРЕТЬ ВСЕ ПРОЕКТЫ =====
    all_projects = await Project.all()
    print("\nВсе проекты:")
    for p in all_projects:
        print(f"  [{p.id}] {p.title} — {p.category} ({p.year})")
    
    # Закрываем соединение
    await Tortoise.close_connections()

if __name__ == "__main__":
    asyncio.run(main())
```

Запустите:

```powershell
cd backend
uv run python edit_projects.py
```

---

## Структура проекта

### Обязательные поля:

```json
{
  "title": "Название проекта",
  "category": "Разработка | Дизайн | Стартапы | Другое",
  "status": "Завершен | В работе",
  "year": "2024",
  "image": "https://...",
  "description": "Описание"
}
```

### Необязательные поля:

```json
{
  "client": "Название клиента",
  "role": "Ваша роль",
  "images": ["url1", "url2", "url3"]
}
```

---

## 🎨 Где брать изображения?

### Бесплатные стоки:

1. **Unsplash** — https://unsplash.com
2. **Pexels** — https://pexels.com
3. **Pixabay** — https://pixabay.com

### Как получить прямую ссылку на изображение:

#### Unsplash:
1. Откройте изображение
2. Нажмите **"Download"** (правой кнопкой → Копировать адрес ссылки)
3. URL будет вида: `https://images.unsplash.com/photo-...?q=80&w=2000`

#### Ваши собственные изображения:
Загрузите на:
- **ImgBB** — https://imgbb.com (бесплатно)
- **Cloudinary** — https://cloudinary.com
- Или на ваш хостинг

---

## ✅ Проверка изменений

После редактирования проектов:

1. **Перезапустите бэкенд** (если он был запущен):
   ```powershell
   # Остановите (Ctrl+C) и запустите снова
   cd backend
   uv run python -m app.main
   ```

2. **Обновите фронтенд** в браузере (F5 или Ctrl+R)

3. **Проверьте API**:
   - Откройте http://localhost:8000/api/projects
   - Вы должны увидеть обновленный список проектов в JSON

---

## 🐛 Частые ошибки

### ❌ Ошибка: "UNIQUE constraint failed"
**Причина**: Пытаетесь создать проект с уже существующим ID  
**Решение**: Не указывайте `id` при создании нового проекта — он назначится автоматически

### ❌ Ошибка: "Invalid enum value"
**Причина**: Неправильное значение для `category` или `status`  
**Решение**: Используйте только разрешенные значения:
- **category**: `Дизайн`, `Разработка`, `Стартапы`, `Другое`
- **status**: `В работе`, `Завершен`

### ❌ Проект не отображается на сайте
**Причина 1**: Бэкенд не запущен  
**Решение**: Запустите `cd backend && uv run python -m app.main`

**Причина 2**: Фронтенд не обновлен  
**Решение**: Обновите страницу (F5)

**Причина 3**: Проект отфильтрован  
**Решение**: Нажмите "Все" в фильтрах проектов

---

## 📞 Дополнительная помощь

- **API документация**: http://localhost:8000/docs
- **Проверка здоровья API**: http://localhost:8000/api/health
- **Список проектов JSON**: http://localhost:8000/api/projects

---

**Готово!** 🎉 Теперь вы знаете все способы редактирования проектов в вашем портфолио!
