# 🎯 НАЧНИТЕ ОТСЮДА!

Добро пожаловать в ваш обновленный портфолио-сайт! Теперь проекты хранятся в PostgreSQL и загружаются через REST API.

---

## 📦 Что было сделано:

### ✅ Backend (FastAPI + PostgreSQL)
- ✨ Создана полноценная REST API на FastAPI
- 🗄️ Настроена PostgreSQL база данных через Tortoise-ORM
- 🔄 API endpoints для работы с проектами
- 📝 Автоматическая документация API (Swagger)
- 🌱 Скрипт для заполнения БД начальными данными

### ✅ Frontend (React модификации)
- 🔌 API клиент для взаимодействия с бэкендом
- 🎣 Custom React hooks (useProjects, useProject)
- ⏳ Состояния загрузки и обработка ошибок
- 🎨 Плавные анимации загрузки

### ✅ Документация
- 📖 README.md - основная документация
- ⚡ QUICKSTART.md - быстрый старт
- 🚀 DEPLOYMENT.md - руководство по развертыванию
- 🛠️ Скрипты для Windows (PowerShell)

---

## 🚀 Запуск за 5 минут:

### 1. Установите PostgreSQL
Скачайте: https://www.postgresql.org/download/windows/

После установки создайте БД:
```sql
CREATE DATABASE portfolio;
CREATE USER portfolio_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE portfolio TO portfolio_user;
```

### 2. Установите uv (менеджер пакетов Python)
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 3. Настройте Backend
```powershell
cd backend

# Создайте .env файл (скопируйте .env.example)
copy .env.example .env
notepad .env  # Отредактируйте настройки БД

# Установите зависимости
uv sync

# Инициализируйте БД
uv run python -m app.seed_data

# Запустите сервер
uv run python -m app.main
```

Или используйте удобные скрипты:
```powershell
.\init-db.ps1  # Инициализация БД
.\run.ps1      # Запуск сервера
```

### 4. Настройте Frontend (в новом терминале)
```powershell
# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

### 5. Откройте в браузере
- 🎨 Фронтенд: http://localhost:5173
- 🔧 Backend API: http://localhost:8000
- 📚 API Docs: http://localhost:8000/docs

---

## 📚 Полезные ссылки:

- **[QUICKSTART.md](QUICKSTART.md)** - Детальная пошаговая инструкция
- **[README.md](README.md)** - Полная документация проекта
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Как опубликовать сайт в интернет
- **[backend/README.md](backend/README.md)** - Документация по бэкенду

---

## 🎨 Что можно делать:

### Добавить новый проект:

**Через API (http://localhost:8000/docs):**
1. Откройте Swagger UI
2. Найдите `POST /api/projects`
3. Нажмите "Try it out"
4. Заполните данные и отправьте

**Через код:**
```bash
curl -X POST "http://localhost:8000/api/projects" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Мой новый проект",
    "category": "Разработка",
    "status": "В работе",
    "year": "2024",
    "image": "https://example.com/image.jpg",
    "description": "Описание проекта",
    "client": "Название клиента",
    "role": "Ваша роль",
    "images": ["https://example.com/detail1.jpg"]
  }'
```

**Прямо в БД (через pgAdmin):**
```sql
INSERT INTO projects (title, category, status, year, image, description, client, role, images)
VALUES (
  'Мой проект',
  'Разработка',
  'Завершен',
  '2024',
  'https://example.com/image.jpg',
  'Описание проекта',
  'Клиент',
  'Разработчик',
  '["https://example.com/img1.jpg"]'::jsonb
);
```

---

## 🛠️ Структура проекта:

```
my-web-site/
├── backend/              # 🔧 Backend API (FastAPI)
│   ├── app/
│   │   ├── main.py       # Основное приложение
│   │   ├── models.py     # Модели БД
│   │   ├── schemas.py    # Pydantic схемы
│   │   └── seed_data.py  # Заполнение БД
│   ├── .env              # ⚙️ Конфигурация (создайте!)
│   ├── run.ps1           # 🏃 Быстрый запуск
│   └── init-db.ps1       # 📊 Инициализация БД
│
├── src/
│   ├── api/
│   │   └── client.ts     # 🔌 API клиент
│   ├── hooks/            # 🎣 React hooks
│   ├── components/       # 🎨 UI компоненты
│   └── pages/            # 📄 Страницы
│
├── .env                  # ⚙️ Конфигурация фронтенда
├── QUICKSTART.md         # ⚡ Быстрый старт
├── README.md             # 📖 Полная документация
└── DEPLOYMENT.md         # 🚀 Развертывание
```

---

## ❓ Часто задаваемые вопросы:

### Бэкенд не запускается?
1. Проверьте, что PostgreSQL запущен
2. Проверьте настройки в `backend/.env`
3. Убедитесь, что база данных создана

### Фронтенд не видит проекты?
1. Убедитесь, что бэкенд запущен (http://localhost:8000/api/health)
2. Проверьте `.env` в корне проекта
3. Откройте DevTools → Console для просмотра ошибок

### Как изменить дизайн?
- Основной цвет: `#FF4533` в компонентах
- Стили в `components/*.tsx`
- TailwindCSS классы

### Как добавить новую категорию?
1. Обновите `CategoryEnum` в `backend/app/models.py`
2. Обновите тип `Category` в `src/data/projects.ts`
3. Добавьте в массив `categories` в `src/components/Projects.tsx`

---

## 🎉 Готово!

Теперь у вас есть полнофункциональный портфолио-сайт с:
- ✅ Динамической загрузкой проектов из БД
- ✅ REST API для управления контентом
- ✅ Современным React интерфейсом
- ✅ Готовностью к развертыванию

### Следующие шаги:
1. 🎨 Добавьте свои реальные проекты
2. 🖼️ Замените изображения на свои
3. ✏️ Настройте контент под себя
4. 🚀 Опубликуйте сайт (см. DEPLOYMENT.md)

**Удачи!** 🚀

---

💡 **Подсказка**: Если что-то не работает, смотрите [QUICKSTART.md](QUICKSTART.md) для детальных инструкций.
