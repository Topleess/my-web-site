# ⚡ Быстрый старт

Это краткое руководство для быстрого запуска проекта локально.

## Шаг 1: Подготовка

### Установите необходимое ПО:

1. **Node.js** (18+): https://nodejs.org/
2. **PostgreSQL** (13+): https://www.postgresql.org/download/
3. **uv** (менеджер пакетов Python):
   ```powershell
   powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
   ```

## Шаг 2: Настройка базы данных

Откройте **psql** или **pgAdmin** и выполните:

```sql
CREATE DATABASE portfolio;
CREATE USER portfolio_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE portfolio TO portfolio_user;
```

## Шаг 3: Бэкенд

```powershell
# Перейдите в папку backend
cd backend

# Скопируйте .env.example в .env
copy .env.example .env

# Откройте .env и настройте подключение к PostgreSQL
notepad .env

# Установите зависимости
uv sync

# Инициализируйте базу данных (создаст таблицы и добавит тестовые данные)
uv run python -m app.seed_data

# Запустите бэкенд
uv run python -m app.main
```

✅ Бэкенд запущен на http://localhost:8000
📚 API документация: http://localhost:8000/docs

## Шаг 4: Фронтенд

Откройте **новый** терминал:

```powershell
# Убедитесь, что вы в корневой папке проекта
cd my-web-site

# Скопируйте .env.example в .env
copy .env.example .env

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

✅ Фронтенд запущен на http://localhost:5173

## Шаг 5: Готово! 🎉

Откройте браузер и перейдите на http://localhost:5173

Вы должны увидеть ваш портфолио-сайт с проектами, загруженными из базы данных!

---

## 🚨 Проблемы?

### Бэкенд не запускается:

**Ошибка подключения к БД:**
- Убедитесь, что PostgreSQL запущен
- Проверьте настройки в `backend/.env`
- Проверьте, что база данных `portfolio` создана

**ModuleNotFoundError:**
```powershell
cd backend
uv sync
```

### Фронтенд показывает ошибки:

**"Failed to fetch" или CORS ошибки:**
- Убедитесь, что бэкенд запущен (http://localhost:8000/api/health должен отвечать)
- Проверьте `.env` файл в корне проекта (должен быть `VITE_API_URL=http://localhost:8000`)
- Перезапустите фронтенд после изменения .env

**Модули не найдены:**
```powershell
npm install
```

### База данных пустая:

```powershell
cd backend
uv run python -m app.seed_data
```

---

## 📝 Следующие шаги

Теперь можете:
- ✏️ Добавить свои проекты через API: http://localhost:8000/docs
- 🎨 Кастомизировать дизайн в компонентах React
- 📊 Посмотреть проекты в БД через pgAdmin
- 🚀 Изучить [DEPLOYMENT.md](DEPLOYMENT.md) для публикации в интернет

---

## 🛠️ Быстрые команды

### Бэкенд:
```powershell
cd backend

# Запуск
uv run python -m app.main

# Пересоздать БД
uv run python -m app.seed_data

# Проверка API
curl http://localhost:8000/api/health
```

### Фронтенд:
```powershell
# Dev сервер
npm run dev

# Сборка для продакшна
npm run build

# Предпросмотр сборки
npm run preview
```

---

Готово! Если остались вопросы, смотрите подробный [README.md](README.md) 📖
