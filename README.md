# 🎨 Личный портфолио-сайт

Современный портфолио-сайт с динамической загрузкой проектов из базы данных PostgreSQL.

## 🏗️ Архитектура

- **Frontend**: React + TypeScript + Vite + TailwindCSS
- **Backend**: FastAPI + Tortoise-ORM + PostgreSQL
- **Стиль**: Современный минималистичный дизайн с анимациями

## ✨ Особенности

- 🎯 Динамическая фильтрация проектов по категориям
- 📱 Полностью адаптивный дизайн
- ⚡ Быстрая загрузка и плавные анимации
- 🗄️ Проекты хранятся в PostgreSQL
- 🔄 REST API для управления проектами
- 🎨 Детальные страницы проектов с галереями

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- Python 3.11+
- PostgreSQL 13+
- uv (менеджер пакетов Python)

### Установка

#### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd my-web-site
```

#### 2. Настройка бэкенда

```bash
cd backend

# Установите uv (если еще не установлен)
# Windows PowerShell:
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# Создайте .env файл
copy .env.example .env

# Отредактируйте .env с вашими настройками PostgreSQL
notepad .env

# Установите зависимости
uv sync

# Инициализируйте базу данных
uv run python -m app.seed_data

# Запустите бэкенд
uv run python -m app.main
```

Бэкенд будет доступен на `http://localhost:8000`

API документация: `http://localhost:8000/docs`

#### 3. Настройка фронтенда

```bash
# Вернитесь в корневую папку
cd ..

# Создайте .env файл
copy .env.example .env

# (Опционально) Отредактируйте .env если бэкенд на другом порту
notepad .env

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

Фронтенд будет доступен на `http://localhost:5173`

## 📁 Структура проекта

```
my-web-site/
├── backend/                # Backend API
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py        # FastAPI приложение
│   │   ├── models.py      # Модели БД
│   │   ├── schemas.py     # Pydantic схемы
│   │   ├── database.py    # Настройка БД
│   │   ├── config.py      # Конфигурация
│   │   └── seed_data.py   # Скрипт для заполнения БД
│   ├── pyproject.toml
│   ├── .env.example
│   └── README.md
│
├── src/
│   ├── api/
│   │   └── client.ts      # API клиент
│   ├── components/        # React компоненты
│   │   ├── Header.tsx
│   │   ├── Hero.tsx
│   │   ├── Projects.tsx
│   │   ├── Contact.tsx
│   │   └── Footer.tsx
│   ├── hooks/             # Custom React hooks
│   │   ├── useProjects.ts
│   │   └── useProject.ts
│   ├── pages/             # Страницы
│   │   ├── Home.tsx
│   │   └── ProjectPage.tsx
│   ├── data/
│   │   └── projects.ts    # TypeScript типы
│   ├── App.tsx
│   └── index.tsx
│
├── .env.example
├── package.json
├── tsconfig.json
├── vite.config.ts
├── DEPLOYMENT.md          # Руководство по развертыванию
└── README.md
```

## 🔧 Разработка

### Backend API Endpoints

- `GET /api/projects` - Получить все проекты
  - Query params: `category`, `status`, `limit`
- `GET /api/projects/{id}` - Получить конкретный проект
- `POST /api/projects` - Создать новый проект
- `GET /api/categories` - Получить категории с количеством

### Добавление нового проекта

#### Через API:

```bash
curl -X POST "http://localhost:8000/api/projects" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Новый проект",
    "category": "Разработка",
    "status": "В работе",
    "year": "2024",
    "image": "https://example.com/image.jpg",
    "description": "Описание проекта",
    "client": "Клиент",
    "role": "Разработчик",
    "images": ["https://example.com/image1.jpg"]
  }'
```

#### Напрямую в БД:

```sql
INSERT INTO projects (title, category, status, year, image, description, client, role, images)
VALUES (
  'Новый проект',
  'Разработка',
  'В работе',
  '2024',
  'https://example.com/image.jpg',
  'Описание проекта',
  'Клиент',
  'Разработчик',
  '["https://example.com/image1.jpg"]'::jsonb
);
```

## 🎨 Кастомизация

### Цвета

Основной акцентный цвет: `#FF4533` (красно-оранжевый)

Измените в компонентах или добавьте в tailwind.config.js:

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#FF4533',
      },
    },
  },
}
```

### Категории проектов

Категории определены в `backend/app/models.py`:

```python
class CategoryEnum(str, Enum):
    DESIGN = "Дизайн"
    DEVELOPMENT = "Разработка"
    STARTUPS = "Стартапы"
    OTHER = "Другое"
```

Для добавления новой категории:
1. Обновите enum в `models.py`
2. Обновите тип `Category` в `data/projects.ts`
3. Добавьте в массив `categories` в `components/Projects.tsx`

## 📦 Сборка для продакшна

### Frontend

```bash
npm run build
```

Готовые файлы будут в папке `dist/`

### Backend

```bash
cd backend
uv sync --frozen
```

Запустите с помощью:
```bash
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

Подробнее см. [DEPLOYMENT.md](DEPLOYMENT.md)

## 🐛 Отладка

### Backend не запускается:

1. Проверьте PostgreSQL:
   ```bash
   psql -U your_user -d portfolio
   ```

2. Проверьте .env файл

3. Проверьте логи:
   ```bash
   cd backend
   uv run python -m app.main
   ```

### Frontend не подключается к API:

1. Убедитесь, что бэкенд запущен: `http://localhost:8000/api/health`

2. Проверьте `VITE_API_URL` в `.env`

3. Проверьте CORS настройки в `backend/app/main.py`

4. Откройте DevTools → Console/Network для просмотра ошибок

## 📝 TODO / Будущие улучшения

- [ ] Админ-панель для управления проектами
- [ ] Аутентификация для защищенных endpoints
- [ ] Загрузка изображений на сервер
- [ ] Теги для проектов
- [ ] Поиск по проектам
- [ ] Пагинация
- [ ] Оптимизация изображений
- [ ] SEO оптимизация
- [ ] Analytics интеграция

## 🤝 Контакты

Если возникли вопросы или нужна помощь:

- Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) для инструкций по развертыванию
- Посмотрите документацию API: `http://localhost:8000/docs`
- Проверьте логи бэкенда и консоль браузера

## 📄 Лицензия

MIT
