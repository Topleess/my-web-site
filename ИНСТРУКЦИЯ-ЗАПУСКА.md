# 🚀 Пошаговая инструкция запуска

## 📋 Что нужно сделать ОДИН РАЗ (первоначальная настройка)

### Шаг 1: Установите необходимое ПО

#### 1.1. PostgreSQL
Скачайте и установите: https://www.postgresql.org/download/windows/

При установке:
- Запомните пароль для пользователя `postgres`
- Оставьте порт по умолчанию: `5432`

#### 1.2. uv (менеджер пакетов Python)
Откройте PowerShell и выполните:
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

Закройте и откройте PowerShell заново после установки.

#### 1.3. Node.js (если еще не установлен)
Скачайте: https://nodejs.org/ (выберите LTS версию)

---

### Шаг 2: Создайте базу данных PostgreSQL

#### Вариант A: Через pgAdmin (графический интерфейс)

1. Откройте **pgAdmin** (установился вместе с PostgreSQL)
2. Подключитесь к серверу (введите пароль postgres)
3. Правой кнопкой на **Databases** → **Create** → **Database**
   - Database: `portfolio`
   - Owner: `postgres`
   - Save
4. Правой кнопкой на **Login/Group Roles** → **Create** → **Login/Group Role**
   - General → Name: `portfolio_user`
   - Definition → Password: `ваш_пароль` (запомните!)
   - Privileges → включите все галочки
   - Save

#### Вариант B: Через командную строку

Откройте PowerShell и выполните:
```powershell
# Откройте psql (может потребовать пароль postgres)
psql -U postgres
```

В psql консоли выполните:
```sql
CREATE DATABASE portfolio;
CREATE USER portfolio_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE portfolio TO portfolio_user;
\q
```

---

### Шаг 3: Настройте Backend

```powershell
# Перейдите в папку проекта
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site"

# Перейдите в папку backend
cd backend

# Создайте .env файл из примера
copy .env.example .env

# Откройте .env для редактирования
notepad .env
```

В файле `.env` измените:
```env
DATABASE_URL=postgres://portfolio_user:ваш_пароль@localhost:5432/portfolio
DB_USER=portfolio_user
DB_PASSWORD=ваш_пароль
```

Сохраните файл (Ctrl+S) и закройте.

```powershell
# Установите зависимости Python
uv sync

# Инициализируйте базу данных (создаст таблицы и добавит тестовые проекты)
uv run python -m app.seed_data
```

При выполнении `seed_data` вы увидите:
```
Connecting to database...
Generating schemas...
Seeding database with initial projects...
✓ Created: FinTech App
✓ Created: Modern Branding
✓ Created: Eco Startup
✓ Created: Abstract 3D
✓ Created: Crypto Dashboard

✅ Successfully seeded 5 projects!
```

---

### Шаг 4: Настройте Frontend

```powershell
# Вернитесь в корневую папку проекта
cd ..

# Убедитесь что .env файл существует
# (если нет - скопируйте из .env.example)
if (!(Test-Path .env)) { copy .env.example .env }

# Установите зависимости Node.js
npm install
```

---

## ▶️ ЕЖЕДНЕВНЫЙ ЗАПУСК (каждый раз когда хотите работать)

### Терминал 1: Backend

```powershell
# Перейдите в папку backend
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site\backend"

# Запустите backend сервер
uv run python -m app.main
```

Вы увидите:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Database initialized successfully
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

✅ **Backend запущен на:** http://localhost:8000

**Не закрывайте это окно терминала!**

### Терминал 2: Frontend (откройте НОВЫЙ терминал)

```powershell
# Перейдите в папку проекта
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site"

# Запустите frontend dev сервер
npm run dev
```

Вы увидите:
```
VITE v6.2.0  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

✅ **Frontend запущен на:** http://localhost:5173

---

## 🌐 Открытие в браузере

1. Откройте браузер (Chrome, Edge, Firefox)
2. Перейдите на: **http://localhost:5173**
3. Вы увидите ваш портфолио-сайт!

### Полезные ссылки:
- 🎨 **Сайт:** http://localhost:5173
- 🔧 **API:** http://localhost:8000
- 📚 **API Документация:** http://localhost:8000/docs
- ✅ **Проверка API:** http://localhost:8000/api/health
- 📊 **Список проектов (JSON):** http://localhost:8000/api/projects

---

## 🛑 Остановка серверов

### Backend
В терминале с backend нажмите: **Ctrl + C**

### Frontend
В терминале с frontend нажмите: **Ctrl + C**

---

## 🗄️ Редактирование данных в базе данных

### Способ 1: Через pgAdmin (САМЫЙ ПРОСТОЙ) ⭐

1. Откройте **pgAdmin**
2. Подключитесь к серверу PostgreSQL
3. Разверните: **Servers** → **PostgreSQL** → **Databases** → **portfolio** → **Schemas** → **public** → **Tables**
4. Правой кнопкой на **projects** → **View/Edit Data** → **All Rows**

Теперь вы видите все проекты в таблице!

**Добавить новый проект:**
- Нажмите последнюю пустую строку
- Заполните поля (id оставьте пустым - сгенерируется автоматически)
- Для поля `images` используйте формат: `["url1", "url2"]`
- Нажмите **F6** для сохранения

**Редактировать проект:**
- Дважды кликните на ячейку
- Измените значение
- Нажмите **F6** для сохранения

**Удалить проект:**
- Выделите строку
- Нажмите **Delete** → **F6**

### Способ 2: Через SQL запросы

В pgAdmin:
1. Правой кнопкой на **portfolio** → **Query Tool**
2. Вставьте SQL и нажмите **F5**

**Посмотреть все проекты:**
```sql
SELECT * FROM projects;
```

**Добавить новый проект:**
```sql
INSERT INTO projects (title, category, status, year, image, description, client, role, images)
VALUES (
  'Мой новый проект',
  'Разработка',
  'В работе',
  '2024',
  'https://images.unsplash.com/photo-1551288049-bebda4e38f71?w=800',
  'Описание моего проекта',
  'Название клиента',
  'Моя роль',
  '["https://images.unsplash.com/photo-1563986768609-322da13575f3?w=800"]'::jsonb
);
```

**Изменить проект:**
```sql
UPDATE projects
SET 
  title = 'Новое название',
  description = 'Новое описание',
  status = 'Завершен'
WHERE id = 1;
```

**Удалить проект:**
```sql
DELETE FROM projects WHERE id = 1;
```

**Посмотреть только определенную категорию:**
```sql
SELECT * FROM projects WHERE category = 'Разработка';
```

### Способ 3: Через API (Swagger UI)

1. Откройте браузер: http://localhost:8000/docs
2. Найдите **POST /api/projects**
3. Нажмите **Try it out**
4. Заполните JSON:
```json
{
  "title": "Новый проект через API",
  "category": "Дизайн",
  "status": "В работе",
  "year": "2024",
  "image": "https://images.unsplash.com/photo-1600607686527-6fb886090705?w=800",
  "description": "Описание проекта",
  "client": "Клиент",
  "role": "Дизайнер",
  "images": [
    "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?w=800"
  ]
}
```
5. Нажмите **Execute**
6. Проект добавлен! Обновите страницу сайта

---

## 🚀 Развертывание на сервере (чтобы можно было обращаться из интернета)

### Вариант 1: Railway.app (САМЫЙ ПРОСТОЙ)

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Добавьте PostgreSQL (из шаблонов)
4. Добавьте свой backend:
   - Загрузите код через Git или загрузите папку `backend`
   - Railway автоматически определит Python и установит зависимости
5. Добавьте переменные окружения:
   - `DATABASE_URL` - скопируйте из Railway PostgreSQL
   - `ALLOWED_ORIGINS` - URL вашего фронтенда
6. Получите публичный URL для backend

Для фронтенда:
1. Зарегистрируйтесь на https://vercel.com
2. Импортируйте ваш проект
3. Добавьте переменную окружения:
   - `VITE_API_URL` - URL вашего backend с Railway
4. Deploy!

### Вариант 2: Ваш собственный сервер

Полное руководство: **backend/PRODUCTION-DEPLOY.md**

Краткая версия:
1. Арендуйте VPS (например, Timeweb, REG.RU)
2. Установите PostgreSQL, Python, uv, Nginx
3. Загрузите код на сервер
4. Настройте systemd для автозапуска backend
5. Настройте Nginx как reverse proxy
6. Получите SSL сертификат (Let's Encrypt)
7. Настройте фронтенд на другом домене или поддомене

---

## 📊 Структура таблицы projects

| Поле         | Тип        | Описание                                |
|--------------|------------|-----------------------------------------|
| id           | Integer    | Уникальный ID (автоматически)          |
| title        | String     | Название проекта                        |
| category     | String     | Дизайн / Разработка / Стартапы / Другое |
| status       | String     | В работе / Завершен                     |
| year         | String     | Год (например: "2024")                  |
| image        | String     | URL главного изображения                |
| description  | Text       | Описание проекта                        |
| client       | String     | Название клиента (опционально)          |
| role         | String     | Ваша роль (опционально)                 |
| images       | JSONB      | Массив URL доп. изображений             |
| created_at   | Timestamp  | Дата создания (автоматически)           |
| updated_at   | Timestamp  | Дата обновления (автоматически)         |

---

## ❓ Частые проблемы

### Backend не запускается

**Ошибка: "Connection refused" или "Database error"**
- Проверьте, что PostgreSQL запущен
- Откройте pgAdmin и попробуйте подключиться
- Проверьте настройки в `backend/.env`

**Ошибка: "ModuleNotFoundError"**
```powershell
cd backend
uv sync
```

### Frontend не видит проекты

**Проверьте backend:**
```powershell
# Откройте в браузере:
http://localhost:8000/api/health
# Должно вернуть: {"message":"OK"}

http://localhost:8000/api/projects
# Должно вернуть JSON с проектами
```

**Проверьте .env в корне проекта:**
```env
VITE_API_URL=http://localhost:8000
```

**Перезапустите frontend** после изменения .env:
```powershell
# Ctrl+C чтобы остановить
npm run dev  # запустить снова
```

### База данных пустая

```powershell
cd backend
uv run python -m app.seed_data
```

Если спросит "Do you want to clear and reseed?" - введите `yes`

---

## 🎯 Быстрая шпаргалка

### Запуск (каждый раз)
```powershell
# Терминал 1 - Backend
cd backend
uv run python -m app.main

# Терминал 2 - Frontend
cd ..
npm run dev
```

### Добавить проект (быстро)
1. Откройте pgAdmin
2. portfolio → Tables → projects → View/Edit Data
3. Заполните новую строку
4. F6 для сохранения
5. Обновите сайт в браузере

### Полезные ссылки
- Сайт: http://localhost:5173
- API Docs: http://localhost:8000/docs
- Проекты JSON: http://localhost:8000/api/projects

---

**Готово!** Теперь вы знаете как запускать и редактировать ваш сайт! 🎉
