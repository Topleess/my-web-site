# Автоматически сгенерированные зависимости для не-uv окружений
# Для использования с uv: uv sync
# Для использования без uv: pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
tortoise-orm>=0.21.0
asyncpg>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
