# 🚀 Простое развертывание на сервере (для чайников)

## 📋 Что у вас должно быть:

1. **Сервер** (VPS/VDS) с:
   - Linux (Ubuntu 20.04+ или 22.04)
   - SSH доступ
   - Минимум 1GB RAM
   
2. **Домен** (необязательно, можно использовать IP)

---

## 🎯 Общая схема:

```
Ваш компьютер (Windows)
    ↓
Загружаете код на сервер (через Git/FTP/SSH)
    ↓
Настраиваете на сервере (один раз)
    ↓
Сайт работает 24/7
    ↓
Редактируете проекты через:
  • Swagger UI (браузер)
  • pgAdmin (с вашего компа)
  • SSH (терминал)
```

---

## 🔧 Шаг 1: Подготовка сервера (один раз)

### 1.1. Подключитесь к серверу

**Windows (PowerShell):**
```powershell
ssh username@ваш-сервер.com
# Введите пароль
```

### 1.2. Установите необходимое ПО

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите PostgreSQL
sudo apt install postgresql postgresql-contrib -y

# Установите Python 3.11
sudo apt install python3.11 python3.11-venv python3-pip -y

# Установите uv
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc

# Установите Nginx
sudo apt install nginx -y

# Установите Git (для загрузки кода)
sudo apt install git -y
```

---

## 🗄️ Шаг 2: Настройка PostgreSQL

```bash
# Войдите в PostgreSQL
sudo -u postgres psql

# В psql консоли выполните:
CREATE DATABASE portfolio_prod;
CREATE USER portfolio_user WITH PASSWORD 'придумайте_сложный_пароль';
GRANT ALL PRIVILEGES ON DATABASE portfolio_prod TO portfolio_user;
\c portfolio_prod
GRANT ALL ON SCHEMA public TO portfolio_user;
\q
```

---

## 📦 Шаг 3: Загрузка кода на сервер

### Вариант A: Через Git (если код в GitHub/GitLab)

```bash
cd /var/www
sudo mkdir portfolio-backend
sudo chown $USER:$USER portfolio-backend
cd portfolio-backend

# Загрузите код
git clone https://github.com/ваш-username/ваш-репозиторий.git .
# Или скопируйте только папку backend
```

### Вариант B: Через SFTP (FileZilla, WinSCP)

1. Откройте **WinSCP** или **FileZilla**
2. Подключитесь к серверу:
   - Host: `ваш-сервер.com`
   - Port: `22`
   - Protocol: `SFTP`
   - Username/Password: ваши данные
3. Загрузите папку `backend` в `/var/www/portfolio-backend`

### Вариант C: Через SCP (командная строка)

**На вашем компьютере (Windows PowerShell):**
```powershell
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site"
scp -r backend username@ваш-сервер.com:/var/www/portfolio-backend
```

---

## ⚙️ Шаг 4: Настройка бэкенда

```bash
cd /var/www/portfolio-backend

# Установите зависимости
uv sync

# Создайте .env файл
nano .env
```

**Вставьте в .env:**
```env
DATABASE_URL=postgres://portfolio_user:ваш_пароль@localhost:5432/portfolio_prod
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio_prod
DB_USER=portfolio_user
DB_PASSWORD=ваш_пароль

HOST=0.0.0.0
PORT=8000

# Укажите домен фронтенда (или просто * для любых)
ALLOWED_ORIGINS=*

ENV=production
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

```bash
# Инициализируйте базу данных (создаст таблицы и добавит тестовые проекты)
uv run python -m app.seed_data

# Проверьте, что работает
uv run python -m app.main
```

Если видите `Application startup complete` — всё работает!  
Нажмите `Ctrl+C` для остановки.

---

## 🔄 Шаг 5: Автозапуск (systemd)

Чтобы бэкенд работал постоянно, даже после перезагрузки:

```bash
sudo nano /etc/systemd/system/portfolio-api.service
```

**Вставьте:**
```ini
[Unit]
Description=Portfolio Backend API
After=network.target postgresql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/portfolio-backend
Environment="PATH=/home/ваш-username/.local/bin:/usr/bin"
ExecStart=/home/ваш-username/.local/bin/uv run uvicorn app.main:app --host 0.0.0.0 --port 8000

Restart=always

[Install]
WantedBy=multi-user.target
```

**⚠️ Замените `ваш-username` на ваше имя пользователя!**

```bash
# Узнать свое имя пользователя:
whoami

# Найти путь к uv:
which uv

# Настройте права
sudo chown -R www-data:www-data /var/www/portfolio-backend

# Запустите сервис
sudo systemctl daemon-reload
sudo systemctl start portfolio-api
sudo systemctl enable portfolio-api

# Проверьте статус
sudo systemctl status portfolio-api
```

Должно быть `active (running)` зеленым цветом!

---

## 🌐 Шаг 6: Настройка Nginx

```bash
sudo nano /etc/nginx/sites-available/portfolio-api
```

**Вставьте:**
```nginx
server {
    listen 80;
    server_name ваш-домен.com;  # или IP сервера

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

```bash
# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/portfolio-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🎉 Шаг 7: Готово! Проверка

Откройте в браузере:

### Проверка API:
```
http://ваш-домен.com/api/health
```
Должно показать: `{"message":"OK"}`

### Swagger UI (для управления проектами):
```
http://ваш-домен.com/docs
```

Здесь вы сможете добавлять/редактировать/удалять проекты!

---

## 📱 Шаг 8: Загрузка фронтенда

### На вашем компьютере:

```powershell
cd "C:\Users\alex2\Desktop\my-web-site (MAIN)\my-web-site"

# Создайте .env с URL вашего API
echo "VITE_API_URL=http://ваш-домен.com" > .env

# Соберите фронтенд
npm run build
```

Папка `dist` будет содержать готовый сайт.

### Загрузите на сервер:

**Через SCP:**
```powershell
scp -r dist/* username@ваш-сервер.com:/var/www/html/
```

**Или через FTP:** загрузите содержимое папки `dist` в `/var/www/html/`

### Настройте Nginx для фронтенда:

```bash
sudo nano /etc/nginx/sites-available/default
```

**Замените содержимое на:**
```nginx
server {
    listen 80 default_server;
    server_name ваш-домен.com www.ваш-домен.com;
    
    root /var/www/html;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

```bash
sudo nginx -t
sudo systemctl restart nginx
```

---

## ✅ ВСЁ ГОТОВО!

Теперь ваш сайт доступен:
- **Фронтенд**: http://ваш-домен.com
- **API**: http://ваш-домен.com/docs

---

## 🛠️ Как редактировать проекты после развертывания

### Способ 1: Через браузер (САМЫЙ ПРОСТОЙ)

Откройте: `http://ваш-домен.com/docs`

Найдите `POST /api/projects` → Try it out → вставьте JSON → Execute

### Способ 2: Через pgAdmin (с вашего компьютера)

1. Установите pgAdmin: https://www.pgadmin.org/download/
2. Создайте новое подключение:
   - Host: `ваш-сервер.com`
   - Port: `5432`
   - Database: `portfolio_prod`
   - Username: `portfolio_user`
   - Password: `ваш_пароль`

⚠️ **Возможно, нужно открыть порт 5432 в firewall сервера:**
```bash
sudo ufw allow 5432/tcp
```

3. Откройте таблицу `projects` и редактируйте как Excel!

### Способ 3: Через SSH

```bash
ssh username@ваш-сервер.com
cd /var/www/portfolio-backend

# Создайте скрипт для добавления проекта
nano add_project.py
```

Вставьте:
```python
import asyncio
from tortoise import Tortoise
from app.config import settings
from app.models import Project

async def main():
    await Tortoise.init(
        db_url=settings.DATABASE_URL,
        modules={"models": ["app.models"]}
    )
    
    project = await Project.create(
        title="Новый проект",
        category="Разработка",
        status="Завершен",
        year="2024",
        image="https://images.unsplash.com/photo-...",
        description="Описание",
        client="Клиент",
        role="Роль",
        images=[]
    )
    print(f"Создан проект: {project.title} (ID: {project.id})")
    
    await Tortoise.close_connections()

asyncio.run(main())
```

```bash
uv run python add_project.py
```

---

## 🐛 Проблемы?

### Бэкенд не запускается:
```bash
sudo journalctl -u portfolio-api -f
```

### Посмотреть порты:
```bash
sudo netstat -tulpn | grep :8000
```

### Перезапустить бэкенд:
```bash
sudo systemctl restart portfolio-api
```

### Проверить логи Nginx:
```bash
sudo tail -f /var/log/nginx/error.log
```

---

## 🔒 Опционально: HTTPS (SSL)

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получите бесплатный SSL сертификат
sudo certbot --nginx -d ваш-домен.com
```

Certbot автоматически настроит HTTPS!

---

## 📝 Чек-лист

- [ ] Сервер с Linux
- [ ] SSH доступ работает
- [ ] PostgreSQL установлен
- [ ] Python 3.11 + uv установлены
- [ ] Nginx установлен
- [ ] База данных создана
- [ ] Код загружен на сервер
- [ ] .env файл настроен
- [ ] База заполнена данными (`seed_data`)
- [ ] systemd сервис создан и запущен
- [ ] Nginx настроен
- [ ] API отвечает (проверка /api/health)
- [ ] Фронтенд загружен и работает
- [ ] Можете добавлять проекты через /docs

---

**Готово!** 🎉 Ваш сайт развернут и работает 24/7!

Для редактирования проектов используйте Swagger UI или pgAdmin — никакого кода писать не нужно!
